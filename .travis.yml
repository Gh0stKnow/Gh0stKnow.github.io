<code class="language-yum hljs http">language: node_js
node_js: stable

# S: Build Lifecycle
install:
  - npm install


#before_script:
 # - npm install -g gulp

script:
  - hexo clean
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "ghostknow"
  - git config user.email "kidteaing@gmail.com"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - HexoSource
env:
 global:
   - GH_REF: github.com/Gh0stKnow/Gh0stKnow.github.io.git
